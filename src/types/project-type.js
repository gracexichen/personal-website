class ProjectType {
	constructor(i, name, url, description) {
        this.index = i;
		this.projectName = name;
		this.url = url;
		this.description = description;
	}
}

export default ProjectType;
